#include <iostream>
#include <vector>
#include <numeric>	// ???????????????????¨?????¨??????¨
using std::cout;
using std::cin;
using std::endl;
using std::vector;

int main() {
	int r, c;
	//int sumR, sumC;	// ??????????¨???¨??????????¨?
	// ?????¨????????°???????????????
	cin >> r >> c;
	vector<vector<int>> data;
	data.resize(r);
	for (int i = 0; i < r; i++)
	{
		data[i].resize(c);
	}

	// ??\???
	for (int i = 0; i < r; i++)
	{
		for (int j = 0; j < c; j++)
		{
			cin >> data[i][j];
		}
	}

	// ?¨??????¨??????
	// ??????????????¨??????????¨?
	int sum;
	for (int i = 0; i < data.size(); i++)
	{
		sum = 0;
		for (int j = 0; j < data[0].size(); j++)
		{
			cout << data[i][j] << " ";
			sum += data[i][j];
		}
		cout << sum << endl;
	}

	// ??????????¨???¨????????????????¨?
	int Asum = 0;
	for (int i = 0; i < data[0].size(); i++)
	{
		sum = 0;
		for (int j = 0; j < data.size(); j++)
		{
			sum += data[j][i];
		}
		cout << sum << " ";
		Asum += sum;
	}
	cout << Asum << endl;

	/*
	// ??????????¨????
	for (int i = 0; i < r; i++)
	{
		cout << accumulate(data[i].begin(), data[i].end(), 0) << endl;;
	}
	cout << "**********************" << endl;
	int sum = 0;
	// ??????????¨????
	for (int i = 0; i < r; i++)
	{
		for (int j = 0; j < r; i++)
		{
			sum += data[j][i];
		}
		cout << sum << endl;
	}
	*/
	/*
	// ??????
	for (int i = 0; i < r; i++)
	{
		for (int j = 0; j < c; j++)
		{
			cout << data[i][j];
		}
		cout << endl;
	}
	*/
	return 0;
}