#include <stdio.h>
#include <vector>
using namespace std;

typedef vector<int> Row;
typedef vector<Row> Matrix;

Matrix scan_matrix(int r, int c)//?????????????????????
{	
	Matrix matrix;
	
	for(int i = 0; i < r; i++){	
		Row row;//???
		for(int j = 0; j < c; j++){
			int a;
			scanf("%d", &a);
			row.push_back(a);
		}
		matrix.push_back(row);
	}
	return matrix;
}

void print_matrix(Matrix matrix)//???????????????
{
	int r = matrix.size();
	for(int i = 0; i < r; i++){	
		Row row = matrix[i];
		int c = row.size();
		for(int j = 0; j < c; j++){
			if(j == 0){
				printf("%d", row[j]);
			}
			else{
				printf(" %d", row[j]);
			}
		}
		printf("\n"); 
	}
}

Matrix add_sum(Matrix matrix)//????????????????????????????¨?????????????
{	
	//???????????????????¨??????????
	int r = matrix.size();
	for(int i = 0; i < r; i++){	
		Row row = matrix[i];//???
		int row_sum = 0;	//??????????¨?
		int c = row.size();	//?????°
		for(int j = 0; j < c; j++){
			row_sum += row[j];
		}
		matrix[i].push_back(row_sum);
	}
	
	//?????????????¨?????????????????????????
	Row row = matrix[0];
	int c = row.size();	//?????°
	Row retsu_sum(c);	//??????????¨?
	for(int i = 0; i < r; i++){
		for(int j = 0; j < c; j++){
			retsu_sum[j] += matrix[i][j];
		}
	}
	
	matrix.push_back(retsu_sum);
	
	return matrix;
}

int main(void)
{
	vector<Row> matrix;
	
	int r, c;//r???c???
	scanf("%d %d", &r, &c);
	
	matrix = scan_matrix(r, c);
	matrix = add_sum(matrix);
	print_matrix(matrix);
}