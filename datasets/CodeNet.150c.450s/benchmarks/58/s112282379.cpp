#include <string>
#include <stack>
#include <iostream>

using namespace std;

int main() {
    string word;
    stack<int> S;
    while(cin >> word){
        if (word == "+"){
            // ??°??????????¶???????????????\??????
            int n = S.top();
            S.pop();
            n = n + S.top();
            S.pop();
            S.push(n);
        }
        else if (word == "-"){
            // ??°??????????¶???????????????\??????
            int n = S.top();
            S.pop();
            n = S.top() - n;
            S.pop();
            S.push(n);
        }
        else if (word == "*"){
            // ??°??????????¶???????????????\??????
            int n = S.top();
            S.pop();
            n = n * S.top();
            S.pop();
            S.push(n);
        }
        else {
            // word?????°???????????\??????
            S.push(stoi(word));
        }
 }
    // S??????????????¨???
    cout << S.top() << endl;
}