#include <iostream>
#include <algorithm>
#include <vector>
using namespace std;

long n, sum;
vector<long> v;
int main(void){
	cin >> n;

	for (int i = 0; i < n; ++i){
		long input;
		cin >> input; sum += input;
		v.push_back(input);//v.pushback(X)???vector???v??????????°????X??????????????????
	}

	sort(v.begin(), v.end());//??????
	cout << v.front() << " " << v.back() << " " << sum << endl;//????????¨??????
	
	//system("pause");
	return 0;
}