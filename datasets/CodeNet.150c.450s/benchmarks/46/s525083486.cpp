#include <iostream>
using namespace std;

// 2??°?????????sum??¨??????????????????????????°?????¢???
int nsum_two(int m, int n, int sum)
{
    int nsum = 0;
    for (int i = m; i < (sum / 2.0); i++)
    {
        if ((sum - i) > n) continue; //?????§?????????n????¶?????????????????????§??????
        nsum++;
    }
    return nsum;
}
        
int main()
{
    while (true)
    {
        int n, x;
        cin >> n >> x;
        if (n == 0 && x == 0) break;
        int nsum_three = 0;
        for (int j = 1; j < (x / 3); j++)
        {
            nsum_three += nsum_two(j + 1, n, x - j);
        }
    cout << nsum_three << endl;
    }
    return 0;
}