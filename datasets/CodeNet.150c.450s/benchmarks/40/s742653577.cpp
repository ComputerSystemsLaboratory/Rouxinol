#include <iostream>
#include<cstdio>
#include <cmath>
#include <string>
using namespace std;

#define FACE 6

int DiceOctcome(struct Dice *p);

struct Dice {
	int a[FACE];
	string direction;
};

int main()
{
	int i;
	struct Dice dice;
	for(i=0;i<FACE;i++)scanf("%d", &dice.a[i]);
	

	cin >> dice.direction;
	printf("%d\n", DiceOctcome(&dice));

	return 0;
}

int DiceOctcome(struct Dice *p) {

	int octcome;//??????????¨???????
	int  b[FACE];
	octcome = p->a[0];//??????????????????????????????
	
	for (int i = 0; i < FACE; i++)  b[i] = p->a[i];
	//???????????????p???????????????????§???????Dice???dice?????????a[6]?????????????????§???????????????
	for (int i = 0; i <= (p->direction.size()-1); i++) {
		//string??????swtich????????????????????£?????????
		if (p->direction[i] == 'N') {
			p->a[0] = b[1];
			p->a[4] = b[0];
			p->a[5] = b[4];
			p->a[1] = b[5];
			octcome = p->a[0];
		}
		else if (p->direction[i] == 'W') {
			p->a[2] = b[5];
			p->a[0] = b[2];
			p->a[3] = b[0];
			p->a[5] = b[3];
			octcome = p->a[0];
		}
		else if (p->direction[i] == 'E') {
			p->a[2] = b[0];
			p->a[5] = b[2];
			p->a[3] = b[5];
			p->a[0] = b[3];
			octcome = p->a[0];
		}
		else {
			p->a[0] = b[4];
			p->a[1] = b[0];
			p->a[5] = b[1];
			p->a[4] = b[5];
			octcome = p->a[0];
		}

		for (int i = 0; i < FACE; i++)  b[i] = p->a[i];

	}
	
	return octcome;
}