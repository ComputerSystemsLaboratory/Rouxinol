#include<iostream>
#include<cstdio>
#define a(i,b) for(int i=0;i<b;i++)
using namespace std;
int main() {
    int n,c;
    while(scanf("%d",&n)!=EOF){
    	c=0;
    	a(i,10)a(j,10)a(k,10)a(h,10)if(i+j+k+h==n)c++;
    cout<<c<<endl;
    }
    return 0;
}