//???????¨???????,??£??????????????????
#include <bits/stdc++.h>
using namespace std;

#define FOR(i,a,b) for (int i=(a);i<(b);i++)
#define RFOR(i,a,b) for (int i=(b)-1;i>=(a);i--)
#define REP(i,n) for (int i=0;i<(n);i++)
#define RREP(i,n) for (int i=(n)-1;i>=0;i--)

const int MAX_N = 100;
vector<int> r(MAX_N);
vector<int> c(MAX_N);
//DP????????????
//DP[i,j]???i????????????j????????????????????????????????£?????¨??????????°????????????°
vector<vector<int> > DP(MAX_N, vector<int>(MAX_N,-1));

int dev(int k, int l){
	//?¨????????????????????????´???
	if(DP[k][l]<0){
		int mini=INT_MAX;
		if(k==l){
			mini=0;
		}
		FOR(i,0,l-k){
			int left = dev(k,k+i);
			int right = dev(k+i+1,l);
			mini=min(mini, left + right + r[k]*c[k+i]*c[l]);
		}
		DP[k][l]=mini;
	}
	return DP[k][l];	
}

int main(){
	int n;
	cin>>n;
	REP(i,n){
		cin>>r[i]>>c[i];
	}
	int res = dev(0,n-1);
	/*
	REP(i,n){
		REP(j,n){
			cout<<"["<<i<<"]["<<j<<"] "<<DP[i][j]<<endl;
		}
	}*/
	cout<<res<<endl;
	return 0;
}