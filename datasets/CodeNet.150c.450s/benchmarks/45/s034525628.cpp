#include<iostream>
using namespace std;

static const int c = 1000000007;

long long power(long long m, long long n) {
	if (n == 0) return 1;
	return n % 2 == 0 ? power(m * m % c, n / 2) % c : m * power(m * m % c, n / 2) % c; //????????????????????°
}

int main() {
	int m, n; //?????£??????int??§??§??????
	cin >> m >> n;
	cout << power(m, n) << endl;

	return 0;
}