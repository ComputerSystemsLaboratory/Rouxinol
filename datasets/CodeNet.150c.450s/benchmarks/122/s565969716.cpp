#include <iostream>
#include <vector>
#include <string>
#include <cassert>
using namespace std;
#define TOPINDEX 1
#define DUMMYVALUE 0

void Insert(vector<int>& Heap, int k)
{
	Heap.push_back(k);
	int InsertIndex = Heap.size() - 1;
	int ParentIndex = (Heap.size() - 1) / 2;
	while(Heap[InsertIndex] > Heap[ParentIndex] && InsertIndex > TOPINDEX){
		swap(Heap[InsertIndex], Heap[ParentIndex]);
		InsertIndex = ParentIndex;
		ParentIndex = InsertIndex / 2;
	}
}

void maxHeapify(vector<int>& A, int i)
{
	int H = A.size() - 1;
	int l = 2 * i;
	int r = 2 * i + 1;
	//????????????????????????????????§???????????§????????????????????¶
	int largest;
	if(l <= H && A[l] > A[i]){
		largest = l;
	}else{
		largest = i;
	}
	if(r <= H && A[r] > A[largest]){
		largest = r;
	}
	//i???????????????????????§????????´???
	if(largest != i){
		swap(A[i], A[largest]);
		//?????°??????????????????
		maxHeapify(A, largest);
	}
}

void ExtractMax(vector<int>& Heap)
{
	int TailIndex = Heap.size() - 1;
	swap(Heap[TailIndex], Heap[TOPINDEX]);
	cout << Heap[TailIndex] << endl;
	Heap.pop_back();
	maxHeapify(Heap, TOPINDEX);
}

int main()
{
	/* ??????
	MaxHeap?????¢??§?????????
	1??????Exract????????????????????¨????°??????\????????????????°??????????????¶????
	??????????????????????????????MaxHeapify????????¨??????
	1??????Insert????????????????°????????????????????°???????????????????????????£??????????????????????????°??????
	(??????????????¨????????¢????????????????????????????????????????????§?????????)
	*/
	vector<int> Heap(1, DUMMYVALUE);
	while(true){
		string Command;
		cin >> Command;
		if(Command == "insert"){
			int k;
			cin >> k;
			Insert(Heap, k);
		}else if(Command == "extract"){
			ExtractMax(Heap);
		}else{
			break;
		}
	}
	return 0;
}