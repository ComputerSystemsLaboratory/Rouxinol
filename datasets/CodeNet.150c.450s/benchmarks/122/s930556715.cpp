#if 0
15?????????.????????£????????????????????£???.
#endif

#include <queue>
#include <iostream>
#include <string>
using namespace std;

int main() {
  string cmd; // while????????´????????????????????????????????°???????????¨??????
  priority_queue<int> Q;
  while (cin >> cmd && cmd != "end") {
    if (cmd == "insert") {
      int k;
      cin >> k;
      Q.push(k);
    } else if (cmd == "extract") {
      int n = Q.top();
      Q.pop();
      cout << n << endl;
    }
  }
}