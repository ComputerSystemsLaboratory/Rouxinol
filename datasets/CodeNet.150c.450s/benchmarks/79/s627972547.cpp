//Hanafuda Shuffle
#include <iostream>
#include <algorithm>
using namespace std;
int N, R, p, c;
int main() {
  while (cin >> N >> R && N>0) { //1??????????????????
    int A[N];
    for (int i=0; i<N; ++i) { //??±????????\???
      A[i] = N-i;
    }
    for (int i=0; i<R; ++i) { //?????£?????????
      cin >> p >> c;
      rotate(A, A+p-1, A+p+c-1);
    }
    cout << A[0] << endl;
  }
}