#include <iostream>
#include <algorithm>

using namespace std;

#define rep(i,n) for(int i = 0; i < int (n) ; i++)

int main(){
    int n,r,p,c; //r?????????????????°???p????????£?????????????????????????????????????????????c????????£???????????????????????°
    
    while(cin >> n >> r && n!=0 && r != 0){
        int cards[n];
        
        rep(i,n) cards[i] = n-i;
    
        //???????????£?????°
        rep(i,r){
            cin >> p >> c;
            rotate(cards, cards+p-1, cards+p-1+c);
            //rotate(??????????????????????????????,?????¢??°,??¨????????????????????????) *(start<=?<end)??????????????????+????????¨?????¢??°???-1?????????
        }
        
        cout << cards[0] << endl;
    }
}