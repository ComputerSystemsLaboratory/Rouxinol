#if 0
  30 ????¨???????
  ???????????????????????????????????£????????§ http://judge.u-aizu.ac.jp/onlinejudge/review.jsp?rid=1874216#1 ???????????????????????????
  r ??????????????? 1<<30 ?????????????????¨ 2 ????????????????????±????????? 6 ??§?????? 5 ????????£????????????????????????
#endif
#include <cstdio>
#define rep(i,a) for(int i=0;i<(a);++i)

const int MAX_N = 100001;

int n, k, w[MAX_N];

bool C( int P )
{
  int tr = 0, cnt = 1;

  rep( i, n )
  {
    if( tr+w[i] <= P && cnt <= k )
      tr += w[i];
    else if( w[i] <= P && cnt <= k-1 )
    {
      tr = w[i];
      ++cnt;
    }
    else
      return false;
  }

  return true;
}

int main()
{
  scanf( "%d%d", &n, &k );
  rep( i, n )
    scanf( "%d", w+i );

  // [l, r]
  int l = 0, r = 1e9;
  while( l < r )
  {
    int mid = (l+r)/2;

    if( C( mid ) )
      r = mid;
    else
      l = mid+1;
  }

  printf( "%d\n", l );

  return 0;
}