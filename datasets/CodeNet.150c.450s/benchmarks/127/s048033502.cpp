#include<iostream>
#include<string>
#include<set>
#include<algorithm>
#define P(r)s.insert(a+b),s.insert(b+a),reverse(r.begin(),r.end())
using namespace std;int main(){string t,a,b;for(cin>>t;cin>>t;){set<string>s;for(int i=0;++i<t.size();P(a),P(b),P(a),P(b))a=t.substr(0,i),b=t.substr(i);cout<<s.size()<<endl;}}