#if 0
set???????????´?????\??£???????????§??????????????????????´????????????????set?????¨????????????
#endif

#include <string>
#include <set>
#include <iostream>
#include <algorithm>
using namespace std;

int main(){
	int n;
	string t, t1, t2, r1, r2; //??\???(??????),????????????,????????????,???????????¢,???????????¢
	typedef set<string> SET; //????????´?????\??£???????????§??????????????????????´?
	cin>>n;
	for(int k=0;k<n;++k){
		cin>>t;
		SET T;
		for(int i=0;i<t.length();++i){
				t1="";
				t2="";
			for(int j=0;j<=i;++j){ //??????????????????
				t1+=t[j];
			}
			for(int j=0;j<t.length()-1-i;j++){
				t2+=t[t.length()-1-j]; //??????
			}
			r1=t1;
			reverse(r1.begin(),r1.end()); //?????¢
			r2=t2;
			reverse(r2.begin(),r2.end()); //?????¢
			T.insert(t1+t2);
			T.insert(t2+t1);
			T.insert(t1+r2);
			T.insert(r2+t1);
			T.insert(t2+r1);
			T.insert(r1+t2);
			T.insert(r1+r2);
			T.insert(r2+r1);
			//result[i]=T.size();
			}
		cout << T.size() << endl;
	//for(int i=0;i<n;++i)
	}
	return 0;
}