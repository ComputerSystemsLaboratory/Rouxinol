#include <stdio.h>
#include <ctype.h>

int main(void)
{
  char str;
  int num;
  int cou[26];
  int i;
  char alp;

  for(i=0; i<26; i++){
    cou[i] = 0;
  }
  
  while(scanf("%c",&str) != EOF){ //End Of File?????§????????????
    if(isalpha(str)){ //??¢????????????????????????
      if(isupper(str)){ //??¢???????????????????????§????????????
	str = tolower(str); //?°??????????????????????
      }
      num = str - 'a'; //??????str[i]????????????????????????
      cou[num]++; //??????????????????
    }
  }

  for(i=0,alp='a'; i<26; i++,alp++){
    printf("%c : %d\n",alp,cou[i]);
  }
       	
  return(0);
}