#include <stdio.h>
#include <ctype.h>
 
#define N (26)
 
int main(void)
{
    int a[N] = { 0 };       // ??¢?????????????????????'a'-'z'??????26??????
    // char ch[1201];       // ???????????????????????§??????????????????
    int ch, i;
 
    //input
    while ((ch = getchar()) != EOF) {
        //??°???
        if (islower(ch)) {
            a[ch - 'a']++;
        }
        if (isupper(ch)) {
            a[ch - 'A']++;
        }
    }
 
    //output
    for (i = 0; i < N; i++) {
        printf("%c : %d\n", i + 'a', a[i]);  //0+'a' = 'a' ?????????0=a, 1=b, 2=c, ... ?????????
    }
    return 0;
}