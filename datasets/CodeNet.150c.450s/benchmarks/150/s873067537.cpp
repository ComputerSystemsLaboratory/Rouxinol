#include<iostream>
#define MAX 2000000
#define k 10000
using namespace std;
void Counting_Sort(int A[],int B[],int n);

int A[MAX],B[MAX],C[MAX];;
int main()
{
	int n,i,x;

	cin >> n;
	for(i=0;i<n;i++){
		cin >> x;
		A[i] = x;
	}

	Counting_Sort(A,B,n);

	for(i=0;i<n-1;i++){
		cout << B[i] << ' ' ;
	}

	cout << B[i] << endl ;

	return 0;
}

void Counting_Sort(int A[],int B[],int n)
{
    int i,j;

	for(i=0;i<k;i++){
        C[i] = 0 ;

	}

    for(j=0;j<n;j++){
        C[A[j]] = C[A[j]]+1 ;
	}

    /* C[i]??????i???????????属????即???????????????????*/
    
	for(i=1;i<k;i++){
		C[i] = C[i] + C[i-1] ;
	}
    /* C[i]??????i??\????????属???????????属????即???????????????????*/
    for(j=n-1;j>=0;j--){
		C[A[j]] = C[A[j]]-1 ;
		B[C[A[j]]] = A[j] ;
      
	}
}