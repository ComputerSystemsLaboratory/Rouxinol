#if 0
????????£????????????20m
que
#endif

#include <queue>
#include <string>
#include <iostream>
using namespace std;

int main(){
	int n,q;
	queue<string>name;
	string a;
	queue<int>time;
	int b;
	queue<string>result_name;
	queue<int>result_time;
	int count=0; //????????????
	cin >> n >> q;
	for(int i=0; i<n; ++i){
		cin >> a >> b;
		name.push(a);
		time.push(b);
	}
	while(! name.empty()){ //name???????´??????????????????????
		if(time.front()<=q){ //1?????????????????????????????´???
			count+=time.front();
			result_name.push(name.front());
			result_time.push(count);
			name.pop();
			time.pop();
		}else{ //???????????????
			count+=q;
			name.push(name.front()); //???????°??????????
			time.push(time.front()-q); //??¢????????????????????????????????????????°?
			name.pop();
			time.pop();
		}
	}
	for(int i=0; i<n; i++){  //??¨?????¨???
		cout << result_name.front() << " "<< result_time.front() <<endl;
		result_name.pop();
		result_time.pop();
	}
}