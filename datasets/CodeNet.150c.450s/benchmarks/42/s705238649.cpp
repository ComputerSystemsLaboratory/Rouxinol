#include <iostream>
#include <queue>
#include <string>

int main()
{
    std::queue<std::string> process;
    std::queue<int> left_time;
    
    int number_process, max_time;
    std::cin >> number_process >> max_time;//?????????????????°??¨??????????????????????????????????????????
    
    for(int i = 0; i < number_process; i++) {//???????????????????????????????????±???????????????
        std::string process_name;
        int process_time;
        
        std::cin >> process_name >> process_time;
        
        process.push(process_name);
        left_time.push(process_time);
    }
    
    int left_process = number_process;
    
    while(left_process != 0) {//????????????????????????????????§??°?????????
        static int now_time = 0;
        std::string now_process;
        int now_process_time;
        
        now_process = process.front();
        process.pop();
        
        now_process_time = left_time.front();
        left_time.pop();
        
        if(now_process_time > max_time) {//?????§????????????????????????????????£???????????£????????????????????????
            now_process_time -= max_time;//?????????????????????
            process.push(now_process);//?????\?????????????????\?????????
            left_time.push(now_process_time);
            
            now_time +=max_time;//max_time????????????????????????
        } else {
            now_time += now_process_time;//??????????????????????????????????????????
            
            std::cout << now_process << " " << now_time << std::endl;//????????£????????§??????
            
            left_process--;//?????£???????????????????????°?????????????????????
        }
    }
    
    return 0;
}