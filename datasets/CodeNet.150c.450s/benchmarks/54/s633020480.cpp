#include<iostream>
#include<string>
#include<vector>
using namespace std;

string Transition(string Word){
	for(int i = 0; i < Word.size(); i++){
		if('a' <= Word[i] && Word[i] <= 'z') Word[i] += 'A' - 'a';
	}
	return Word;
}

vector<string> ReadWords(vector<string> Sentence){
//	?????????????????????????????????END_OF_TEXT??§???????????°??§???????????´?????????????????????????????????
	while(1){
		string Go;
		cin >> Go;
		if(Go == "END_OF_TEXT") break;
		for(int i = 0; i < Go.size(); i++){
			if('a' <= Go[i] && Go[i] <= 'z') Go[i] += 'A' - 'a';
		}
		Sentence.push_back(Go);
	}
	return Sentence;
}

int Count(string Word, vector<string> Sentence){
//	?????????????????????????????????????????????????????°?????????
	int C = 0;
	for(int i = 0; i < Sentence.size(); i++){
		if(Sentence[i] == Word) C++;
	}
	return C;
}

int main(){
//	??????????????????????????§???????????´??????
	string Word;
	cin >> Word;
	Word = Transition(Word);
//	????????????????????????
	vector<string> Sentence;
	Sentence = ReadWords(Sentence);
//	for(int i = 0; i < Sentence.size(); i++){
//		cout << Sentence[i];
//	}
//	?????????????????????????????????????????°??????????????????
	int X = Count(Word, Sentence);
	cout << X << endl;
	
//	system("pause");
	return 0;
}