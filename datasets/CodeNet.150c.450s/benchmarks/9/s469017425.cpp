#include <iostream>
#include <string>

using std::string;
using std::cin;
using std::cout;
using std::endl;

// ?????£???????????¢??°
string shuffle(string s, int n) {
	string left;
	string right;
	string s1;
	// ?????´
	left = s.substr(0, n);
	//cout << "left:" << left << endl;
	// ?????´
	right = s.substr(n, s.size());
	//cout << "right:" << right << endl;
	// ??£???
	s1 = right + left;
	return s1;
}

int main() {
	string a;	// ?????????
	int length;	// ?????£??????????????°
	int num;	// ?????£????????????????????°
	while (1)
	{
		cin >> a;
		if (a == "-") break;
		cin >> length;
		//cout << "length:" << length << endl;
		for (size_t i = 0; i < length; i++)
		{
			cin >> num;
			a = shuffle(a, num);			
		}
		cout << a << endl;
	}
	return 0;
}