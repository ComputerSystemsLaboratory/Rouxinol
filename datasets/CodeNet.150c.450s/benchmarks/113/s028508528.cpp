#include<cstdio>
#include<algorithm>
using namespace std;

int main() {
	//freopen("test.txt", "r", stdin);
	int num, cas = 1;
	while (~scanf("%d", &num) && num) {
		printf("Case %d: %d\n", cas++, num);
	}
	return 0;
}