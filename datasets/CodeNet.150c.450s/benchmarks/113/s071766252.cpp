#include <bits/stdc++.h>
using namespace std;
typedef long long ll;


int main(void) {
	cin.tie(0);
	ios::sync_with_stdio(false);
	
	for(int i = 1; ; i++) {
		int x;
		cin >> x;
		if(x == 0) break;
		cout << "Case " << i << ": " << x << "\n";
	}

	return 0;
}