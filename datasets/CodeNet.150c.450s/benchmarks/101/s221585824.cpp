#include<iostream>
#include<vector>
using namespace std;
void numbering(int v, int k, vector<vector<int>>const&graph, vector<int>&id) {
	
	id[v] = k;
	//v????????????????????????
	//id??????????????°?????????????????????
	//k????????????????????????
	for (auto to : graph[v]) {
		//for(int i=0;i<graph[v].size();++i)
		
		if (id[to] != -1) {
			continue;
		}
		numbering(to, k, graph, id);
	}
}
int main() {
	int n, m;
	cin >> n >> m;
	vector<vector<int>>graph(n);
	for (int i = 0;i < m;++i) {
		int u, v;
		cin >> u >> v;
		graph[u].push_back(v);
		graph[v].push_back(u);
	}
	vector<int>id(n, -1);
	for (int i = 0;i < n;i++) {
		if (id[i] == -1) {
			numbering(i, i, graph, id);
			//numbering(?????????????????????,?????????????????????,graph,id)
		}
	}
	int q;
	cin >> q;
	while (q--) {
		int u, v;
		cin >> u >> v;
		if (id[u] == id[v]) {
			cout <<"yes" << endl;
		}
		else {
			cout << "no" << endl;
		}
	}

}