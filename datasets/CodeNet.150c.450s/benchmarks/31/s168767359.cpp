/***
*	?????§?????????
****/

#include <iostream>
#include <vector>
using namespace std;

int main(void)
{
	int  n;
	cin >> n ;
	vector<int> fxR(n);
	
	for(int t=0; t< n; t++){
		cin >> fxR[t];
	}
	
	int maxR = fxR[1] - fxR[0];
	int minR = fxR[0];
	
	for(int j =1; j<n ; j++){	//n???????????????????????°??????????¢????
		if( maxR < (fxR[j]-minR)){
			maxR = fxR[j]-minR;
		}
		if( minR > fxR[j]){
			minR = fxR[j];
		}
	}
	
	cout << maxR << endl;//?????§???????????¨?????????

	return 0;
}