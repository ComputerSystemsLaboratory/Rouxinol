#include<bits/stdc++.h>
using namespace std;
#define rep(i,a,b) for(int i=a;i<b;i++)





int N, W;
int v[100], w[100];
//-----------------------------------------------------------------
int dp[101][10001]; // dp[i][j] := i???????????§??????????????????????¨????j?????¨????????????????????§???
int main() {
	cin >> N >> W;
	rep(i, 0, N) cin >> v[i] >> w[i];

	// ?????????
	rep(i, 0, 101) rep(j, 0, 10001) dp[i][j] = -1;
	dp[0][0] = 0; // 0??????(?????????)??§?????????????¨????0?????¨?????????????????????????????????

	rep(i, 0, N) rep(j, 0, W + 1) if (0 <= dp[i][j]) {
		dp[i + 1][j] = max(dp[i + 1][j], dp[i][j]); // i????????????????????????
		if (j + w[i] <= W) { // i?????????????????????????????????????¨????W????¶????????????????
			dp[i + 1][j + w[i]] = max(dp[i + 1][j + w[i]], dp[i][j] + v[i]);
		}
	}

	// dp[N][0] ??? dp[N][W]??????????????§????????????
	int ans = -1;
	rep(j, 0, W + 1) ans = max(ans, dp[N][j]);
	cout << ans << endl;
}