#include <iostream>
using namespace std;

long long cnt = 0; //for counting num of invensions
const int INF = 2147483647;

void merge(int *a, int left, int mid, int right) {

    int nL = mid - left;
    int nR = right - mid;

    int L[nL+1], R[nR+1];

    for(int i=0; i<nL; i++) {
        L[i] = a[left + i];
    }
    for(int i=0; i<nR; i++) {
        R[i] = a[mid + i];
    }
    L[nL] = R[nR] = INF;
    

    int indexL = 0,  indexR = 0;
    for(int i = left; i<right; i++) {

        if( L[indexL] <= R[indexR] ) {
            a[i] = L[indexL];
            indexL++;
            cnt += indexR; //L[]?????????????????¨??????????????§???R[]???????????????????????????????´?????????°(indexR)?????°????????°??????????????¢?????°????????????
                          //????????°????????????????°????????????????a[]???????????????????????¨??????????????§?????????????????????????????????????????¨????????????????????????????????????
        } else {
            a[i] = R[indexR];
            indexR++;
        }
        
    }

//    //display info. about this step
//    cout << "-----\n";
//    cout << "merge(" << left << ", " << mid << ", " << right <<")" << endl;
//
//    cout << "L" << " = "; for(int i=0; i<nL+1; i++) {cout << L[i] << " ";} cout << endl;
//    cout << "R" << " = "; for(int i=0; i<nR+1; i++) {cout << R[i] << " ";} cout << endl;
//    
//    cout << "cnt = " << cnt << endl;

    return;
}


void mergeSort(int *a, int left, int right) {

    if(left+1  <  right) {
        int mid = (left + right) / 2;

        mergeSort(a, left, mid);
        mergeSort(a, mid, right);

        merge(a, left, mid, right);

    }

    return;
}


int main(void) {
    int N;
    cin >> N;

    int a[N];
    for(int i=0; i<N; i++) {
        cin >> a[i];
    }

//    //display a[] before sorting
//    cout << "a[] = ";
//    for(int i=0; i<N; i++) {
//        cout << a[i] << " ";
//    }
//    cout << endl;

    //merge sort a[]
    mergeSort(a, 0, N);

//    //display a[] after sorting
//    cout << "a[] = ";
//    for(int i=0; i<N; i++) {
//        cout << a[i] << " ";
//    }
//    cout << endl;
    

//    cout << "Num of inversions = ";
    cout << cnt << endl;
    
    return 0;
}