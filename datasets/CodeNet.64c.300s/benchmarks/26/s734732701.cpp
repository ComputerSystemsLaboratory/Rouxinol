/*
?????????: Elementary Number Theory - Power
URL: http://judge.u-aizu.ac.jp/onlinejudge/description.jsp?id=NTL_1_B&lang=jp
?????°:
18:51:44 ??????????????§?????????????????????????????¨???
18:56:25 ??????????????°??§?????????????????£?????????????????¢????????????????????????????????§?????????
19:01:47 ??????????????£????°??????????
*/
#include <iostream>
using namespace std;

/* n < 1000^3 < 1024^3 = 2^30 */

long long table[32];
// table[k] = pow(m, pow(2,k));
// table[0] = pow(m,1);
// table[1] = pow(m,2);

long long mul(long long a, long long b){
	return (a*b) % 1000000007;
}


signed main()
{
	long long m; long long n;
	cin >> m >> n;

	long long tmp = m;
	for(int i=0;i<32;i++){
		table[i] = tmp;
		tmp = mul(tmp,tmp);
	}

	// 1010 multiplies table[1] and table[3]

	long long ans = 1;
	for(int i=0; i<32; i++){
		if((n >> i) % 2) { // bit????????£??????
			ans = mul(ans, table[i]);
		}
	}
	cout << ans << endl;
}