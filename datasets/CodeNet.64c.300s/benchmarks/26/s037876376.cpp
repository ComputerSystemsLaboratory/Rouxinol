#include <cstdio>
typedef long long int ll;
const ll MOD = 1000000007LL;

ll mod_pow(int n, int k, int mod) {
    // ret := ?????????mul := n ??? 2 ?????? (????????? 2^0 ???)
    ll ret = 1, mul = n;
    for(int i=0; i<31; i++) {
        // ?????????????????£?????????????????°???mul ?????????????????????
        if(k >> i & 1) ret = (ret * mul) % MOD;
        // 2 ??????????????? (n ??? 2^x ??? -> n ??? 2^(x+1) ???)
        mul = (mul * mul) % MOD;
    }
    return ret;
}

int main() {
    int N, k; scanf("%d%d", &N, &k);
    printf("%lld\n", mod_pow(N, k, MOD));
    return 0;
}