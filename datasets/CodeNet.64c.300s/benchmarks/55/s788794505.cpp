#include <bits/stdc++.h>
using namespace std;
int main() {
	int a, b;
	for (a=0; a<10000; a++ ){
		cin >> b;
		if ( b == 0 ) break;
		else {
		cout<< "Case " << a+1 << ": " << b << endl;} 
	}
	return 0;
}

