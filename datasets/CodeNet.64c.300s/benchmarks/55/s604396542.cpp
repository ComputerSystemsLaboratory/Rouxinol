#include <bits/stdc++.h>
#define cinf(n,x) for(int i=0;i<(n);i++) cin >> x[i];
typedef long long int ll;
using namespace std;

int main(){
	int i=1;
	while(1){
		int x; cin >>x;
		if(x ==0)break;
		cout << "Case" << " "<<i<<":"<<" "<<x<<endl;
		i++;
	}
}
