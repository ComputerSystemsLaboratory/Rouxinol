#include <iostream>
#include <cmath>

using std::cout;
using std::cin;
using std::endl;
using std::pow;

int main(void) {
    int x;
    cin >> x;  
    int tri = pow(x, 3);
    cout << tri << endl;
    return 0;
}