#include <iostream>


int main()
{
    int H,W,x,y,r;
    bool in;

    std::cin >> W;
    std::cin >> H;
    std::cin >> x;
    std::cin >> y;
    std::cin >> r;

    //?????????????????¢???????????????
    if (x < 0 || x > W || y < 0 || y > H) {
        in = false;
    } else if ((y - r) < 0 || (y + r) > H) { //??????????????¨?????????????????????
        in = false;
    } else if ((x - r) < 0 || (x + r) > W) {
        in = false;
    } else {
        in = true;
    }


    if (in) {
        std::cout << "Yes" << std::endl;
    } else {
        std::cout << "No" << std::endl;
    }
    return 0;
}