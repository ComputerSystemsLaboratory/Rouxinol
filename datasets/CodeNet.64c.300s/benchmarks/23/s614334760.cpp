#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

int main()
{
  int a = 0, b = 0;
  fscanf(stdin, "%d %d", &a, &b);
  fprintf(stdout, "%d %d", a * b, 2 * a + 2 * b);

  fprintf(stdout, "\n");
  return 0;
}