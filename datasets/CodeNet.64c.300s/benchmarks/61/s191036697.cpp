#include<iostream>
using namespace std;
int main(void) {
    // ???????????????????????????
    int r, c;
    cin >> r >> c;
    // ????´?????????????????????????????????????????????????
    int clm[c];
    for (int i = 0; i < c; i++)
        clm[i] = 0;
    int row = 0;
    for (int i = 0; i < r; i++) {
        int sum = 0;
        for (int j = 0; j < c; j++) {
            int tmp;
            cin >> tmp;
            cout << tmp << " ";
            clm[j] += tmp;
            sum += tmp;
        }
        cout << sum << endl;
    }
    for (int i = 0; i < c; i++) {
        cout << clm[i] << " ";
        row += clm[i];
    }
    cout << row << endl;
    return 0;
}